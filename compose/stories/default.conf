server {
    listen       8002;
    server_name  localhost;

    location = /robots.txt {
        alias /usr/share/nginx/robots.txt;
    }

    location / {
        alias /usr/share/nginx/stories/;
    }
}
